language: c
addons:
  apt:
    packages:
     - libgmp-dev 
     - linux-kernel-headers
     - build-essential
     - kernel-package
     - libssl-dev 

env:
  global:
   # The next declaration is the encrypted COVERITY_SCAN_TOKEN, created
   #   via the "travis encrypt" command using the project repo's public key
   - secure: "aJhRUD5fzRgKHk56CVYVpEWaGaAbvz3Z7MqJRuGedvwlS9Et8Gc4z2u/rEtGqG+8DD6k6s7h5IUmp0Q6iG8nkzA0trfjH8cwzrkCEoGtUYOjbmRWfNtTToRcbxai+MK0jWf/Bq0NoplS8ZGebhQnFw/zvNfiduEfGCrh9YhPyJQ="

before_install:
  - echo -n | openssl s_client -connect scan.coverity.com:443 | sed -ne '/-BEGIN CERTIFICATE-/,/-END CERTIFICATE-/p' | sudo tee -a /etc/ssl/certs/ca-

addons:
  coverity_scan:
    project:
      name: "PeterHuewe/tpm-emulator"
      description: "PeterHuewe/tpm-emulator"
    notification_email: peterhuewe@gmx.de
    build_command_prepend:  mkdir -p build && cd build 
    build_command: cmake -DBUILD_DEV=OFF ../ && make
    branch_pattern: coverity_scan

script:  mkdir -p build && cd build && cmake -DBUILD_DEV=OFF ../ && make && cd ..
